package Room;

public enum RoomType {
    SINGLE,
    DOUBLE
}

// see wtf this is meant to mean
// @Override
// public String toString() {
//     return name().charAt(0) + name().substring(1).toLowerCase();
// }
